Javascript print function----

1. alert(" ")
2. Document.write()
3. innerhtml()

JS Roadmap----

. JS & features
. Variables in JS
. Data types
. Operators
. Conditional & Control statements
. Loops
. Data structures

. Scope
. Objects
. Functions
. Form Validations
. DOM Manipulation
. Events
. BOM Elements

JS Advanced----

. Prototyping
. Proto type Inheritance
. Call backs
. IIFE
. Promises
. Ajax/ Axioxs
. Async Await
. Closures
. OPPs Concepts
. JSON

Variables in JS---

. Var
. Let
. Const

Arrays in JS----

. Why arrays
. How to declare arrays
. How to access array elements
. How to update / add array elements
. Methods on arrays
  -> push()
  -> pop()
  -> Unshift()
  -> shift()
  -> indexOf()
  -> Slice()


Functions in JS----

. What is function-- function{ stat1, stat2, stat3...... statn }
. Why function
. How to create functions---- function functionName(parameter1,.... parametern){ body  return value; }
. How to call function
. What is function expresion--- Assigning function to a variable [add = fun sum(num1,num2){ var result = num1+num2; return result; }


Types of Functions-----

. Named functions
. Anonymous function----- [function(num1,num2){ body return value; } ]
. Immediately Invoked function expression(IIFE)--- When we want to execute a function immediately where they Created, IIFE used. [syntax : (function definition)();]
. Arrow function---- function(par1, par2.... par n)=>{ body return value; }

. if only one parameter

var cube = num1 => num1*num1*num1

var cube = (num1) => num1*num1*num1

var cube = (num1) => {return num1*num1*num1}

. if no argument

var greet = () => console.log("GM");

var greet = _ => console.log("GM");


Global scope && Local scope && Block level scope


25-04-2025----

Default para & Rest para JS----

fun prod(num1,num2){
var result - num1 * num2;
return result;
}

Default param---

fun prod(num1,num2=1){
var result = num1*num2;
return result;
}

Rest param---

We can make function to accept unspecified number of parameters

fun prod(num1=1,num2=3, ...arr){
var result = num1*num2;
return result;
}

fun arguments ---- fun prod(num1,num2,...arr){console.log(arguemnts)} prod(2,3,4,5);


ForEach in JS------

For every element present inside array the specified function will be executed.

syntax:

	arr.forEach(function funName(currentItem, index, arr){
		// code here
	});



Sets in JS------

collection of unique values

// array-- collection of values

======================================================

Objects in JS-----*****

. Object is collection of elemnts in the form of properties end methods.
. Property is a key-value pair.

1. Object Literal

How to access object values:

We can access values by using keys in 2 ways--

1. obj["key"]  -> quotes are mandatory

2. obj.key = value ---> Ex: movie.budget = "400 crores"

==========================================

2. Using a new keyword with Object constructor ====

... let movie1 = new Object();

3. Using a new keyword with a constructor function====

..... step1: create constructor function:

function user(name, age, place){
this.name = name;
this.age = age;
this.place = place
}


step2: create object with constructor function call:

let movie1 = new movie("abc", 26, "Visakhapatnam"):

=========================================

4. Object.create() method====

let movie2 = Object.create(movie);

let movie3 = Object.create(move, {
		name: {
		value: "rrr"
		},
		Music: {
		value: "Keeravani"
		}

=========================================

5. Classes


===========================================


Console.log--------------------

.. console.log()

.. console.info()

.. console.warn()

.. console.error()

.. console.table()

.. console.clear()

.. console.assert()

.. console.count()

.. console.countreset()

.. console.time()

.. console.Timelog()

.. console.timeend()

.. console.group("Group 1")

console.log("group log1")
console.log("group log1")
console.log("group log1")
.. console.groupend()

======================================

DOM in JS--------------(Document object model)

1. By using HTML DOM JavaScript can access, change or remove any elements of HTML document and also can create new elements at any position.

2. When a webpage loaded, browser create DOM of webpage.

 Struc :-------  DOM ---- Document ---- HTML -------[head, title] ------ [body, h1, div ---- p]

Document Object has ------ . Properties,  . Methods

Using this document object properties & methods we can

	. Select HTML elements
	. Modify HTML elements
	. Remove/delete HTMl elements
	. Create HTML elements
	. Add/Remove/Change styles to HTML elements


Methods to select HTML Elements:  

1. document.getElementById("idname") --- Returns element with the specified id

2. document.getElementByClassName("classname") --- Returns list of all elements belongs to the specified class

3. document.getElementByTagName("tagname") --- Returns list of all elements with specified tag

4. document.querySelector(".class/#id/tagname") ---- Returns the first object matching CSS style selector

5. document.querySelectorAll(".class/#id/tagname) --- Returns all objects Matches the CSS Style Selector

========================================================================

DOM properties to select HTML Elements:

. document.body

. document.head

. document.title

. document.anchors

. document.forms

. document.images

. document.scripts

=======================================================

DOM Manipulation in JS------------------------------------

. How to create HTML elements

. How to set content to element

. How to append a new element

. How to insert an element before another element

. How to remove an element

. How to remove child element

. How to replace an elemnet

============================================================

Add & Change style in JS--------

. How to change style of element

. How to get attribute value of an element

. How to set/change attribute of an element
